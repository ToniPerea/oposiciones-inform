// Development environment
// For local development with EmailJS, run: pnpm set-env
// (reads from .env file and fills in the values below)
export const environment = {
  production: false,
  emailjs: {
    publicKey: '',
    serviceId: '',
    templateId: '',
  },
};
